/**
 * @deprecated Prefer `@/theme` or `@/theme/normalize`.
 * Re-export kept so existing paths keep working.
 */
export {
  fs,
  hs,
  moderateScale,
  moderateVerticalScale,
  mvs,
  S,
  scale,
  sp,
  verticalScale,
  ws,
} from '@/theme/normalize';
