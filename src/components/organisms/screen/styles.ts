import { StyleSheet } from 'react-native-unistyles';

export const styles = StyleSheet.create(_theme => ({
  outer: {
    flex: 1,
  },
  inner: {
    flex: 1,
  },
  scrollContent: {
    flexGrow: 1,
  },
}));
